# ARCHITECTURE — 系统架构

本文档描述**最终目标架构**，并明确区分「当前实现」与「未来规划」。
状态标签：`[已实现]` `[已实现，待架构师门控及 Win7 验收]` `[已设计未实现]` `[规划中]`。阶段 1 已有实现，仍需完成修复轮核对、架构师门控解除与 Win7 E1/E2 实机验收。

## 1. 总体拓扑

```
+--------------------------+          内网 HTTP/HTTPS           +---------------------+
|  Win7 SP1 x64 执行端      | <-------------------------------> |  远程/内网模型服务器  |
|  Python 3.8.10 仅标准库   |     [规划中，阶段 5]               |  （不在本仓库范围）   |
+--------------------------+                                    +---------------------+
```

Win7 端是**纯执行端**：不做模型推理，不做长驻服务（最终形态为 CLI 进程）。

## 2. Win7 端分层架构（最终目标）

```
+---------------------------------------------------------------+
|  cli/            命令行入口与人机交互（ASCII-only 控制台）        |  [规划中]
+---------------------------------------------------------------+
|  agent_loop/     任务编排：指令 -> 工具调用 -> 结果回传            |  [规划中, 阶段6]
+---------------------------------------------------------------+
|  tools/          工具层（Agent 可调用的能力单元）                 |
|    fs_read       文件树遍历 / 编码感知读取           [规划中, 阶段4]|
|    fs_write      补丁写入 / 备份 / 回滚              [规划中, 阶段4]|
|    runner        子进程执行（唯一子进程入口）         [规划中, 阶段2]|
|    test_verify   测试命令运行与结果收集              [规划中]       |
+---------------------------------------------------------------+
|  core/           基础设施                                        |
|    probe             环境能力探测               [已设计未实现, 阶段1] |
|    state_store       SQLite 状态/审计存储    [规划中, 阶段5]      |
|    encoding          编码探测与保持          [规划中, 阶段4]      |
|    pathsafe          路径规范化与工作区边界   [规划中, 阶段4]      |
|    gateway           Model Gateway 通信客户端 [规划中, 阶段3]      |
+---------------------------------------------------------------+
|  Python 3.8.10 标准库（唯一依赖层）                               |
+---------------------------------------------------------------+
```

## 3. 当前实现（截至阶段 0/1）

| 组件 | 状态 | 说明 |
|------|------|------|
| 文档体系 | [已实现] | 本仓库全部 `.md` 文件 |
| win7_agent.probe | [已实现，待架构师门控及 Win7 验收] | 完整设计见 `docs/tasks/PHASE_01_CAPABILITY_PROBE.md`；仅实现阶段 1 的离线 Capability Probe，R1--R8 修复轮待架构师核对，尚未完成 E1/E2 实机验收 |
| 其余全部组件 | [规划中] | 仅在本文档与 ROADMAP 中描述，禁止提前实现 |

## 4. 关键架构原则

1. **能力降级而非崩溃**：所有环境相关功能启动前查询 Probe 报告；能力缺失（如无 Git）时关闭对应功能并记录，不中断进程。
2. **单一子进程入口**：所有子进程调用必须经过 `runner`（阶段 2），保证超时/截断/终止策略统一（AGENTS.md C08/C09）。
3. **写操作三段式**：备份 → 写入 → 校验；任何一步失败自动回滚（阶段 4 落实）。
4. **一切持久化带版本**：JSON 报告带 `report_version`，SQLite 带 `schema_version`，协议带 `protocol_version`。
5. **审计不可旁路**：文件修改与命令执行必须经工具层，工具层强制写审计记录（阶段 5/6 落实）。
6. **控制台 ASCII、文件 UTF-8**：绕开 Win7 cmd CP936 限制（ADR-0006）。
7. **无守护进程、无注册表、无管理员权限**：进程生命周期 = 一次任务；交付即复制目录。

## 5. 目录规划（最终目标，当前只有 docs/）

```
win7-coding-agent/
  AGENTS.md  CLAUDE.md  README.md
  docs/                       # [已实现]
  src/
    win7_agent/
      probe/                  # 阶段 1（APPROVED_FOR_IMPLEMENTATION，包结构见任务文档 §4）
      runner/                 # 阶段 2
      gateway/                # 阶段 3（Model Gateway）
      fs/                     # 阶段 4
      state/                  # 阶段 5
      agent/                  # 阶段 6
  tests/
    unit/probe/               # 阶段 1 单元测试
    integration/probe/        # 阶段 1 集成测试
    win7/                     # Win7 实机验收用例与脚本
  scripts/
    run_probe.bat             # 阶段 1 启动脚本
    run_tests.bat             # 阶段 1 测试脚本
  dist/                       # 阶段 7：离线交付包输出（不入库）
```

约束：`src/` 下任何目录的创建必须对应一个状态为 APPROVED_FOR_IMPLEMENTATION 的阶段任务文档，且路径在其允许清单内（AGENTS.md C14）。

## 6. 数据流（最终目标，简化）

```
远程模型指令 --> gateway --> agent_loop --> tools(fs_read/fs_write/runner)
                                  |                |
                                  v                v
                             state_store <---- 审计记录
                                  |
                                  v
                        结果/报告(JSON) --> gateway --> 远程模型
```

阶段 1 的 Probe 独立于该数据流：它是本地 CLI 一次性执行，输出 JSON 报告文件（唯一权威输出；SQLite 仅作为被探测能力的证据文件，ADR-0014），供人和后续阶段程序消费。

## 7. 非目标（架构级）

- 不做插件系统、不做动态加载第三方代码。
- 不做多任务并发编排（单任务串行执行）。
- 不在 Win7 端存储模型凭据以外的任何敏感信息；凭据管理见 SECURITY.md。
