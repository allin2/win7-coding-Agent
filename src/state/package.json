{
  "name": "win7-agent-state",
  "version": "0.1.0",
  "description": "State & Audit module for Win7 Coding Agent — event store, WAL transactions, backlog, audit export",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "jest --forceExit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.3.3",
    "@types/node": "^16.18.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "@types/jest": "^29.5.0"
  }
}
