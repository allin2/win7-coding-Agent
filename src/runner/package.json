{
  "name": "@win7-agent/runner",
  "version": "0.1.0",
  "description": "Runner interface layer for Win7 Coding Agent — command execution, approval, containment",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=16.17.1"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.3.3",
    "@types/node": "^16.18.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "@types/jest": "^29.5.0"
  }
}
