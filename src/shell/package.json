{
  "name": "@win7-agent/shell",
  "version": "0.1.0",
  "description": "Desktop Shell IPC Schema, Security Policy and Updater for Win7 Coding Agent",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=16.17.1"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "lint": "tsc --noEmit && node --check product/policy.js && node --check product/credential-vault.js && node --check product/desktop-host.js && node --check product/desktop-ipc.js && node --check product/main.js && node --check product/preload.js && node --check product/runner-runtime.js && node --check product/renderer/event-queue.js && node --check product/renderer/gateway-settings-payload.js && node --check product/renderer/runner-log.js && node --check product/renderer/renderer.js"
  },
  "dependencies": {
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "typescript": "~5.3.3",
    "@types/node": "^16.18.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "@types/jest": "^29.5.0"
  }
}
