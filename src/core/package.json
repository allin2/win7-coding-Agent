{
  "name": "@win7-agent/core",
  "version": "0.1.0",
  "description": "Agent Core for Win7 Coding Agent — state machine, policy engine, capability broker",
  "private": true,
  "engines": {
    "node": ">=16.17.1"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "tsc --noEmit",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "typescript": "~5.3.3",
    "@types/node": "^16.18.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "@types/jest": "^29.5.0"
  }
}
