# A3.1 isolated public-model acceptance sample

This directory contains synthetic, non-sensitive code created only for the
Windows 7 A3.1 real-model acceptance run. It is not a user project.

The TypeScript sample normalizes display names and builds an ID-based index.
